 <div class='text-center filter-title'><h1>Featured Skills</h1></div>

<div class="skill">
  {{#if session.currentUser}}
    {{#each}}
      {{skill-item skill=this}}
    {{/each}}
  {{else}}
    Please log-in to see featured skills.
  {{/if}}
</div>
