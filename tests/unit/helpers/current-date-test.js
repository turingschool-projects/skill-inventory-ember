import {
  currentDate
} from 'skill-inventory-ember/helpers/current-date';

module('CurrentDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = currentDate(42);
  ok(result);
});
